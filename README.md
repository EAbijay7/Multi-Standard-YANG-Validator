# Multi-Standard YANG Validator

A standards-aware YANG validation framework for Common/YANG, IETF, BBF, IEEE, and ITU-T workflows.

The repository is being populated from the final cleaned project package. See the project documentation and architecture diagram in `docs/`.
