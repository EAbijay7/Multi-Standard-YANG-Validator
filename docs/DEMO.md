# Demo

1. Install Python dependencies from `requirements.txt`.
2. Run `./run.sh --help` to inspect the command surface.
3. Use the sample YANG modules under `samples/` for validation demonstrations.
4. Review generated validation reports in the configured output location.
